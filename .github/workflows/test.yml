name: test

on:
  push:
  pull_request:
  schedule:
    # NOTE: at AM8:05 everyday by JST
    - cron: '5 23 * * *'

jobs:
  on-macos:
    name: macOS
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - run: make setup
  on-ubuntu:
    name: Ubuntu
    runs-on: ubuntu-latest
    env:
      LANG: C
    steps:
      - uses: actions/checkout@v2
      - run: make setup
